# FitFindr — planning.md

> Complete this document before writing any implementation code.
> Your spec and agent diagram are what you'll use to direct AI tools (Claude, Copilot, etc.) to generate your implementation — the more specific they are, the more useful the generated code will be.
> Your planning.md will be reviewed as part of your submission.
> Update it before starting any stretch features.

---

## Tools

List every tool your agent will use. For each tool, fill in all four fields.
You must have at least 3 tools. The three required tools are listed — add any additional tools below them.

### Tool 1: search_listings

**What it does:**
- Searches local listings database to find secondhand apparel items that match the user's keywords, size, and maximum budget.
- Ranks filteered results based on keyword overlap with item descriptions.

**Input parameters:**
- `description` (str): keywords or style tags that describe what the user is looking for
- `size` (str): sizing of item
- `max_price` (float): maximum budget that user is willing to spend

**What it returns:**
- A list of dictionaries representing matching listings sorted by relevance (highest keyword overlap score first).
- Each dictionary contains the fields: id, title, description, category, style_tags, size, condition, price, colors, brand, platform.

**What happens if it fails or returns nothing:**
- If no item match the criteria, an empty list [] is returned instead of a raised exception.
- The agent loop should understand the empty list as a signal to stop the styling process and return a grounding message.

---

### Tool 2: suggest_outfit

**What it does:**
- Pairs the selected secondhand item with items from the user's current wardrobe to create 1–2 cohesive outfit suggestions using an LLM.
- If the user's wardrobe is empty, it uses the LLM to supply general styling advice and aesthetic vibes for the new item instead.

**Input parameters:**
- `new_item` (dict): dictionary with the new thrifted item that the user is considering
- `wardrobe` (dict): dictionary containing items that user currently owns

**What it returns:**
- A string that specifies a creative outfit combination or styling advice.

**What happens if it fails or returns nothing:**
- If the wardrobe is empty, the tool should pivot to providing general styling advice. Otherwise, it provides aesthetic advice specific to the item that may be thrifted.

---

### Tool 3: create_fit_card

**What it does:**
- Takes a generated outfit suggestion and the details of the thrifted item to compile a trendy, short, and highly shareable social media caption.

**Input parameters:**
- `outfit` (str): description generated by `suggest_outfit` that provides user with caption
- `new_item` (dict): dictionary with the new thrifted item that the user is considering

**What it returns:**
- A casual, authentic 2–4 sentence string optimized to look like an Instagram or TikTok OOTD post caption that incorporates the item name, price, and shopping platform.

**What happens if it fails or returns nothing:**
- If the outfit string is missing, empty, or whitespace-only, the tool guards against crashing by returning a clean, descriptive error message string instead of raising an exception.

---

### Additional Tools (if any)

<!-- Copy the block above for any tools beyond the required three -->

---

## Planning Loop

**How does your agent decide which tool to call next?**
The loop begins by checking the user's incoming query. If it's empty, the system sets an error message and terminates immediately. If the user inputs a valid string, the system extracts parameters like `size` and `max_price` before invoking `search_listings(description, size, max_price)`. If an empty list is returned, an error message is logged to `session["error"]` and execution halts. If the list has matching items, the agent stores them in `session["results"]` and extracts the top item as `session["selected_item"]`. Subsequently, the agent calls `suggest_outfit(new_item, wardrobe)`, which maps items from the user's closet to pair with the thrifted clothing or provides general styling advice if the wardrobe is empty. This output is saved in session["outfit_suggestion"] and passed to `create_fit_card(outfit, new_item)`. If the outfit input is missing or empty, an error string is safely assigned. Once a proper caption is generated and saved to `session["fit_card"]`, the agent terminates and presents the data to the UI.

---

## State Management

**How does information from one tool get passed to the next?**
The agent manages data with a mutable dictionary `session`. It tracks the query, wardrobe, results, selected_item, outfit_suggestion, fit_card, and error. When the user puts their initial query, it extracts necessary details and then updates session's parameters respectively.

---

## Error Handling

For each tool, describe the specific failure mode you're handling and what the agent does in response.

| Tool | Failure mode | Agent response |
|------|-------------|----------------|
| search_listings | No results match the query | Sets `session["error"]` to a helpful message suggesting the user loosen their filters, then exits the loop early to block downstream tool execution. |
| suggest_outfit | Wardrobe is empty | Pivots the LLM prompt to generate general aesthetic guidelines and styling inspiration for the item rather than crashing.|
| create_fit_card | Outfit input is missing or incomplete | Catches the error condition and returns a descriptive error caption string instead of raising an unhandled exception. |

---

## Architecture

                     ┌──────────────────┐
                     │    User Input    │
                     └────────┬─────────┘
                              │  user_query & wardrobe_choice
                              ▼
                     ┌──────────────────┐
              ┌─────►│  Planning Loop   │◄────────────────────────┐
              │      └────────┬─────────┘                         │
              │               │                                   │
              │               │ 1. description, size, max_price   │
              │               ▼                                   │
              │      ┌──────────────────┐                         │
              │      │ search_listings  │                         │
              │      └────────┬─────────┘                         │
              │               │                                   │
              │               ├─────────────────┐                 │
              │               │ [results == []] │ [results >= 1]  │
              │               ▼                 ▼                 │
              │         ┌───────────┐     ┌───────────┐           │
              │         │ Set Error │     │ Set Item  │           │
              │         └─────┬─────┘     └─────┬─────┘           │
              │               │                 │                 │
              │  session      │ session         │ session         │ Updates
              │  ["error"]    │ ["error"]       │ ["selected_item"] Session
              │               ▼                 ▼                 │ State
              │      ┌──────────────┐     ┌───────────┐           │
              │      │ Halt Loop &  │     │  Commit   │           │
              │      │ Return Early │     │ to State  │           │
              │      └──────────────┘     └─────┬─────┘           │
              │                                 │                 │
              │                                 │ 2. selected_item│
              │                                 │    & wardrobe   │
              │                                 ▼                 │
              │                        ┌──────────────────┐       │
              │                        │  suggest_outfit  │       │
              │                        └────────┬─────────┘       │
              │                                 │                 │
              │                                 │ outfit_txt      │
              │                                 ▼                 │
Updates       │                        ┌──────────────────┐       │
Session ──────┼────────────────────────┤    Save State    │───────┤
State         │                        └────────┬─────────┘       │
              │                                 │                 │
              │                                 │ 3. outfit_txt   │
              │                                 │    & item       │
              │                                 ▼                 │
              │                        ┌──────────────────┐       │
              │                        │ create_fit_card  │       │
              │                        └────────┬─────────┘       │
              │                                 │                 │
              │                                 │ caption_txt     │
              │                                 ▼                 │
              │                        ┌──────────────────┐       │
              └────────────────────────┤ Final Save State │───────┘
                                       └────────┬─────────┘
                                                │
                                                │ Full Session Output
                                                ▼
                                    ┌───────────────────────┐
                                    │ UI Panel Presentation │
                                    └───────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│                             SESSION STATE                                │
├──────────────────────────────────────────────────────────────────────────┤
│ - query: str              - selected_item: dict      - fit_card: str     │
│ - wardrobe: dict          - outfit_suggestion: str   - error: str | None │
└──────────────────────────────────────────────────────────────────────────┘

---

## AI Tool Plan

**Milestone 3 — Individual tool implementations:**
- *AI Tool:* Gemini / Claude
- *Inputs:* Complete function docstrings from tools.py, the specific Tool 1, Tool 2, and Tool 3 blocks from planning.md, and the exact JSON schemas for listings.json and wardrobe_schema.json.
- *Expected Output:* Fully implemented python code for `search_listings()`, `suggest_outfit()`, and `create_fit_card()` in tools.py using the Groq client API for LLM generation where required.
- *Verification:* Manually verify that `search_listings` handles case-insensitive size string matching and applies an inclusive max_price filter, check that `suggest_outfit` features a conditional branch evaluating whether wardrobe["items"] length is zero.

**Milestone 4 — Planning loop and state management:**
- *AI Tool:* Gemini / Claude
- *Inputs:* Complete Planning Loop, State Management, and Architecture text and ASCII diagram from planning.md, along with the skeletal definitions in `agent.py` and `app.py`.
- *Expected Output:* Complete implementations for `run_agent()` in agent.py and the `handle_query()` handler function in app.py.
- *Verification:* Print out the mutating session dictionary step-by-step during a test script run to confirm that `session["selected_item"]` matches the top item of `session["results"]` and that data passes correctly into subsequent functions, run python `app.py` and test using the browser interface

---

## A Complete Interaction (Step by Step)

Write out what a full user interaction looks like from start to finish — tool call by tool call. Use a specific example query.

**Example user query:** "I'm looking for a vintage graphic tee under $30. I mostly wear baggy jeans and chunky sneakers. What's out there and how would I style it?"

**Step 1:**
The planning loop extracts `description="vintage graphic tee"`, `size=None`, and `max_price=30.0`. It executes `search_listings("vintage graphic tee", None, 30.0)`. The tool iterates over listings.json. It drops items over $30.00 and ranks the remaining matches based on string token overlaps with titles, descriptions, and style tags. It returns a list containing matching item dictionaries. `session["results"]` saves the list, and `session["selected_item"]` explicitly stores the dictionary object.

**Step 2:**
The planning loop evaluates `session["selected_item"]`. Because it contains a valid dictionary, it proceeds to invoke `suggest_outfit(session["selected_item"], wardrobe)`. Assuming the user keeps "Example wardrobe" selected, the tool checks `wardrobe["items"]` and finds it populated with 10 pieces. It structures an LLM prompt compiling the details of the bootleg graphic tee alongside items like baggy straight-leg jeans and chunky white sneakers. It outputs a string text: "Throw this 2003 Tour Bootleg Tee over your White ribbed tank top and pair it with your Baggy straight-leg dark wash jeans. Tie the look together with your Chunky white sneakers for an effortless Y2K streetwear vibe." This text block is stored directly inside `session["outfit_suggestion"]`.

**Step 3:**
The planning loop confirms `session["outfit_suggestion"]` is valid and calls `create_fit_card(session["outfit_suggestion"], session["selected_item"])`. The prompt passes the text generation from Step 2 along with explicit data points from the item dictionary (title, price, platform). It instructs the LLM to mimic casual social media dialogue. A string is outputed "just thrifted this 2003 tour bootleg graphic tee off depop for only $24 structure is unreal. pairing it with my favorite baggy dark wash denim and chunky sneakers today for that perfect worn-in look 🤙 element in bio". The resulting string commits directly to `session["fit_card"]`.

**Final output to user:**
The agent loop gracefully finishes processing. The Gradio frontend unrolls the session details and projects them into the three designated non-interactive display outputs:
- 🛍️ Top listing found: Displays the structured markdown details for the Graphic Tee — 2003 Tour Bootleg Style ($24.00 on depop).
- 👗 Outfit idea: Displays the full pairing recommendation incorporating the user's baggy jeans and chunky sneakers.
- ✨ Your fit card: Displays the generation ready for copy-pasting into social feeds.

---